fastapi
uvicorn
sqlalchemy
pydantic
python-multipart
deepface
tf-keras
mtcnn
# Pinned: opencv-python 5.0.0 ships without cv2.CascadeClassifier / the
# bundled haarcascade data files in its Windows wheel, which breaks
# DeepFace's "opencv" detector backend entirely (used for event photos).
# 4.10.0.84 is a known-good release that includes both.
opencv-python==4.10.0.84
redis
rq
numpy
# Event-photo face detector (see README "Why insightface for event photos"):
# a lightweight ONNX detector, ~45-50x faster than DeepFace's retinaface
# backend on this project's benchmarks with equivalent accuracy.
insightface
onnxruntime
# PostgreSQL driver for SQLAlchemy. SQLite is still the default for
# zero-setup local dev (see core/settings.py); set DATABASE_URL to a
# postgresql+psycopg2:// URL (docker-compose.yml does this automatically)
# to use Postgres instead — no other code changes needed, the app was
# already written against SQLAlchemy's DB-agnostic API.
psycopg2-binary
# SQLAlchemy Vector(512) column type for EventPhoto.embedding on Postgres
# (see db/orm_models.py). Only active when DATABASE_URL is Postgres — the
# same package works fine even when SQLite is in use, it's just unused.
pgvector
# AWS SDK — used here to talk to MinIO over the S3 API (object storage for
# every uploaded photo, plus presigned upload/download URLs). The same code
# works against real AWS S3 by changing S3_ENDPOINT_URL; boto3 doesn't care
# which S3-compatible service is on the other end.
boto3
